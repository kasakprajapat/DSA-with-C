// write a program for memeory allocation c by calloc function
#include <stdio.h>
#include<stdlib.h>
void main() 
{
    int i,n;
    printf("enter number of element:");
    scanf("%d",&n);
    int *a=(int*)calloc(n,sizeof(int));
    printf("\nenter element:");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        printf("%d",a[i]);
    }

}
